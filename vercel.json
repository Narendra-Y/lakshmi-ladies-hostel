{
  "buildCommand": "pnpm --filter @workspace/hostel-app run build && rm -rf public && cp -r artifacts/hostel-app/dist/public public",
  "outputDirectory": "public",
  "rewrites": [
    {
      "source": "/api/:match*",
      "destination": "https://lakshmi-hostel-api.onrender.com/api/:match*"
    },
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
